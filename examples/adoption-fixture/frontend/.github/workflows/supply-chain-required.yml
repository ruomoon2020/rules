name: Supply chain
on: [pull_request]
permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - run: npm audit --audit-level=high
      - run: npx license-checker --production

