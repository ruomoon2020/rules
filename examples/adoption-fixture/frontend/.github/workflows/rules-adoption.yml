name: Rules adoption
on: [pull_request]
permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - run: python common-governance/scripts/check-project-adoption.py --repo . --stack frontend --level 2

