# Project Rules Overlay

本文件只记录项目事实和通用规则的落地参数，不复制或放宽通用硬规则。

## 真实技术栈与目录

- 应用目录：`<path>`
- 业务源码：`<path>`
- 公共组件：`<path>`
- 样式与主题：`<path>`
- API 契约：`<path or planned>`
- 数据库变更：`<path or tool>`

## 实际验证命令

| 范围 | 命令 | 是否 Required |
|---|---|---|
| 静态检查 | `<command>` | 是/否 |
| 类型或编译 | `<command>` | 是/否 |
| 单元/组件测试 | `<command>` | 是/否 |
| 构建 | `<command>` | 是/否 |
| 契约检查 | `<command or missing>` | 是/否 |

## 目标与迁移

- 当前采纳级别：`Level <n>`
- 目标采纳级别：`Level <n>`
- 存量基线：`<path>`
- 迁移负责人：`<team or role>`
- 复核日期：`YYYY-MM-DD`

## 有时限例外

| 规则 | 原因 | 补偿控制 | 责任人 | 到期时间 | 退出条件 |
|---|---|---|---|---|---|
| `<rule>` | `<reason>` | `<control>` | `<owner>` | `YYYY-MM-DD` | `<condition>` |
