{
  "comment": "业务仓 Maven/Gradle 参考，非可执行 JSON",
  "mvn": {
    "verify": "mvn -B verify",
    "test": "mvn -B test",
    "flyway-mysql": "mvn -B flyway:migrate -Dflyway.configFiles=flyway-mysql.conf",
    "flyway-postgresql": "mvn -B flyway:migrate -Dflyway.configFiles=flyway-postgresql.conf"
  },
  "gradle": {
    "check": "./gradlew check"
  }
}
