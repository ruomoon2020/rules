# 安全策略与漏洞报告

> 目的：提供统一的漏洞/secret 泄露/依赖风险报告入口与响应 SLA，便于把安全门禁从“规则”落到“流程与证据”。

---

## 适用范围
- 所有采用本仓规则包（web-backend / web-front / miniapp）的业务项目
- 规则包本身与治理文档变更的安全风险评估

---

## 报告渠道（统一入口）
请通过以下方式之一报告安全问题（业务仓复制本文件时必须替换为组织真实入口）：
- 安全邮箱：`security@example.com`
- 工单系统：`SEC-` 开头的工单（或内部安全平台）
- （可选）Slack/IM：#security-triage（仅用于快速告知，证据仍需补齐）

报告内容建议至少包含：
- 影响对象/资产：服务名、模块、接口/页面、租户/用户范围（如适用）
- 复现步骤或 PoC（尽量最小化）
- 影响等级自评：Critical/High/Medium/Low（未能判断时也可先不填）
- 发现时间、受影响版本范围、相关提交/配置（如有）
- 证据：日志片段、堆栈、请求/响应摘要（避免粘贴 token/密钥）

---

## 严重级别与响应 SLA（必须对齐供应链基线）
严重级别时限参照：[`docs/supply-chain-baseline.md`](docs/supply-chain-baseline.md)（漏洞 SLA）。

- Critical：7 天（修复/缓解或登记风险 + 安全签字 + 补偿控制）
- High：30 天（同上）
- Medium：90 天或下季度（同上）

> 说明：当无法按期完成修复时，必须走“风险登记 / 豁免（限时）/ 补偿控制”，且不得“无证据一直不修”。

---

## secret 泄露处理（强制流程）
当发现或怀疑 secret 泄露（token、密钥、证书、私有 URL、可还原的凭证等）：
1. 立刻确认泄露范围：涉及哪些仓库/分支/环境/构建流水线
2. 立即轮换并撤销：rotate/revoke 相关凭证，禁用可能已外泄的 token
3. 回溯影响：检查是否已被滥用（认证日志、访问日志、异常行为）
4. 补丁与验证：更新产生泄露的代码/配置，确保 CI/CD 能重新验证通过
5. 记录与复盘：
   - 在 `docs/compliance-evidence-log.md`（或你们内部等价系统）记录关键事实与处置证据
   - 如涉及生产安全事件，按事故复盘模板补充 RCA

> 注意：secret 泄露的处置优先级高于“正常开发节奏”，并要求可追溯证据。

---

## 依赖漏洞（CVE/SCA）处置与豁免
当依赖漏洞需要处置：
- 首选：升级依赖、应用补丁，并通过 Required CI 门禁验证
- 若短期无法修复：必须走豁免流程并做风险登记（限时）

豁免与风险接收的统一口径：
- 走 [`docs/rule-exception-process.md`](docs/rule-exception-process.md)（要求有效期、复查日、风险接受人、补偿控制）
- 高风险依赖漏洞不得“无限期豁免”

豁免记录建议包含：
- CVE/组件名/受影响版本范围
- 已采取缓解手段（配置隔离/限流/WAF/降权限/替代实现等）
- 剩余风险说明与证据链接
- 到期复查时间与 Owner

---

## 证据留痕与审计
建议把以下内容纳入证据留痕（按你们组织实际存储系统替换）：
- 报告单（或工单）编号与关键结论
- 处置动作与验证结果（CI 链接、构建产物）
- 豁免单（如适用）与到期/复查记录

可参考：[`docs/compliance-evidence-log.md`](docs/compliance-evidence-log.md)。

---

## Embargo / 披露（建议）
如果报告在披露前仍处于保密状态：
- 在确认可安全公开前，所有细节仅限安全团队与必要的修复参与方
- 给出修复节奏与预计披露时间窗口（由安全团队与影响 Owner 协商）
