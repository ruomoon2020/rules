# Language

- `shared/` 与 `codex/` 使用中文，便于团队评审和落地。
- `cursor/*.mdc` 的 frontmatter 可使用英文 key，正文使用中文。
- 技术名词保留英文：`uni-app`、`pages.json`、`manifest.json`、`subPackages`、`OpenAPI`、`traceId`。
- 规则使用“必须 / 禁止 / 应该 / 可以”表达强度，避免模糊措辞。
