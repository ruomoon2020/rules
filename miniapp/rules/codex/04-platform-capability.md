# 04 Platform Capability

改平台能力时：

1. 平台差异写在 `src/platform/`，页面只调用统一方法。
2. 敏感能力先过 `privacy` 检查。
3. adapter 返回统一结果，不向页面泄露平台原始错误结构。
4. 微信小程序作为第一阶段基线；其他平台能力必须有 fallback。
5. 支付、订阅消息、分享必须处理取消、拒绝、失败、不可用。
6. 分享、scene、二维码参数必须白名单校验。
