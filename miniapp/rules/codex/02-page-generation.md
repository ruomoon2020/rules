# 02 Page Generation

写页面时：

1. 先确认页面在主包还是分包；新业务页默认分包。
2. 先读 generated 类型和 API 方法。
3. 页面只做生命周期、UI 状态、用户事件。
4. 列表必须处理加载、空、错误、正常、分页到底、下拉刷新。
5. 表单提交必须防重复点击，失败保留输入。
6. 涉及授权、登录、支付、分享时调用 service / adapter，不在页面直接调用平台 API。
7. 离页必须清理 timer、eventBus、轮询、监听器。
